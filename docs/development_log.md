# Development Log

Career Tracker の実装進捗と今後の作業候補をまとめる開発ログです。

## 現在の状態

Phase4 まで完了しています。

MVPの主要機能である、認証、主要データのCRUD、関連表示、応募・選考カンバン、ダッシュボードの実データ表示は実装済みです。

## Phase1: 土台作成

状態: 完了

実装内容:

- Next.js / TypeScript / Tailwind CSS のプロジェクト構成
- Supabase クライアント設定
- `.env.example` 作成
- Supabase Auth によるログイン / ログアウト
- サーバー側でのセッション確認
- 未ログイン時の `/login` 誘導
- ログイン後の Header / Sidebar / Main Content レイアウト
- 主要ページの初期ルート作成

## Phase2: 基本CRUD

状態: 完了

実装内容:

- 転職サービス CRUD
- 会社 CRUD
- 担当者 CRUD
- 求人 CRUD
- 一覧検索 / フィルタ
- 独自バリデーション表示
- 削除確認
- 詳細画面での関連担当者 / 関連求人表示

## Phase3: 転職活動管理の中核

状態: 完了

実装内容:

- 応募・選考 CRUD
- 応募・選考ステータス管理
- 応募・選考一覧 / カンバン表示切り替え
- 面談予定 CRUD
- タスク・期限 CRUD
- 求人、応募・選考、面談予定、タスク間の関連表示

## Phase4: 見える化

状態: 完了

実装内容:

- ダッシュボードの実データ集計
- 応募中件数、面談予定件数、期限切れタスク件数、7日以内の期限タスク件数の表示
- 直近の面談予定表示
- 期限が近いタスク表示
- ステータス別応募件数表示
- 選考カンバンの実データ表示
- 期限切れ / 7日以内などの期限注意表示

補足:

- `requirements.md` には「優先度高の求人」というダッシュボード項目がありますが、現状は独立したダッシュボード枠としては未実装です。Phase5以降のダッシュボード改善候補として扱います。
- 詳細画面からダッシュボードへ直接戻る導線は未実装です。左サイドバーから戻れるため、現状では許容しています。

## Phase5以降の候補

優先候補:

- README / ドキュメントの継続更新
- 詳細画面の戻り導線の整理
- ダッシュボードへの「優先度高の求人」表示追加
- 期限切れタスク一覧のダッシュボード表示
- 一覧画面からのタスク完了切り替え
- バリデーション、空状態、削除確認UIの共通部品化
- デプロイ前のRLS、環境変数、Supabase設定の再確認

将来拡張:

- CSV出力
- 通知機能
- Googleカレンダー連携
- AI求人票解析
- PDF出力
- ユーザー設定画面

## 動作確認コマンド

```bash
npm run typecheck
npm run lint
npm run build
```
